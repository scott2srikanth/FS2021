/*
Use Scanner class from util package 
Use if-else and nested if

Output:
	1) Read an integer(x) using Scanner class.
	2) if x value is odd, display "Hello"
	3) if x vlaue is even and,
			if x is between 0 to 10, display "Hi"
			if x is between 10 to 20, display "Hello"
			if x is > 20, display "Bye"

*/